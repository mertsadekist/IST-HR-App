node_modules/
.env
.env.local
dist/
.DS_Store
*.log

# User-uploaded files — never commit (persisted via the /data/uploads volume).
# Anchored to the repo root so app source folders like client/src/data are NOT ignored.
/uploads/
/data/

# Working scratch dir and stray artifacts
scratch/
*.pdf
IST_HR_System*.html

# Source screenshots for the Knowledge Base — the slugified copies live in
# client/public/kb (which IS tracked & shipped); this raw folder stays local.
/Knowledge Base Images/
_t.mjs

# Source business documents dropped in for reference (offer letters, exports).
# Kept out of the repo deliberately — they are inputs to work, not part of it.
docs/New Documents/
